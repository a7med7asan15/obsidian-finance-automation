{
  "name": "obsidian-finance-automation",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.2",
    "obsidian": "^1.13.1",
    "typescript": "^5.9.3"
  }
}
